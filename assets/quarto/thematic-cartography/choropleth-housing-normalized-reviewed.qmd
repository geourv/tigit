---
title: "Coropleta revisada: percentatge d'habitatge no principal del Tarragonès"
lang: ca
ref: choropleth_housing_normalized_reviewed
format:
  gfm:
    output-file: choropleth-housing-normalized-reviewed.generated.md
execute:
  echo: false
  warning: false
  message: false
execute-dir: project
unaltraweb_compute:
  engine: r
  mode: figure
  inputs:
    - _practices/tigit-projecte-territorial/data/processed/tarragones-boundaries-icgc-20260120.gpkg
    - _practices/tigit-projecte-territorial/data/processed/municipal-indicators-tarragones-2021.csv
  outputs:
    - assets/img/thematic-cartography/choropleth-normalized-reviewed-tarragones-2021.svg
---

```{r}
find_project_root <- function(start) {
  path <- normalizePath(start, winslash = "/", mustWork = FALSE)
  repeat {
    if (file.exists(file.path(path, ".unaltraweb", "computations.yml"))) {
      return(path)
    }
    parent <- dirname(path)
    if (identical(parent, path)) {
      stop("Project root not found: missing .unaltraweb/computations.yml")
    }
    path <- parent
  }
}

root <- find_project_root(getwd())
suppressPackageStartupMessages(library(sf))
suppressPackageStartupMessages(library(ggplot2))
suppressPackageStartupMessages(library(classInt))

gpkg <- file.path(root, "_practices/tigit-projecte-territorial/data/processed/tarragones-boundaries-icgc-20260120.gpkg")
mun <- sf::st_read(gpkg, layer = "municipalities_250k", quiet = TRUE)
comarca <- sf::st_read(gpkg, layer = "comarca_250k", quiet = TRUE)

indicators <- read.csv(
  file.path(root, "_practices/tigit-projecte-territorial/data/processed/municipal-indicators-tarragones-2021.csv"),
  fileEncoding = "UTF-8"
)

## Unió per codi municipal: cal igualar formats.
mun$CODIMUNI <- as.character(mun$CODIMUNI)
indicators$mun_code <- as.character(indicators$mun_code)
indicators$mun_code <- ifelse(nchar(indicators$mun_code) == 5, paste0("0", indicators$mun_code), indicators$mun_code)

d <- merge(mun, indicators, by.x = "CODIMUNI", by.y = "mun_code", all.x = TRUE)
stopifnot(
  nrow(d) == 22,
  !anyNA(d$housing_non_main_pct),
  all(d$indicator_status == "ok")
)

## Exemple revisat: percentatge normalitzat i classes quantils explícites.
cls <- classIntervals(d$housing_non_main_pct, n = 5, style = "quantile")
format_percent <- function(x) formatC(x, format = "f", digits = 1, decimal.mark = ",")
class_labels <- paste0(
  format_percent(head(cls$brks, -1)),
  "–",
  format_percent(tail(cls$brks, -1))
)
d$class <- cut(
  d$housing_non_main_pct,
  breaks = cls$brks,
  labels = class_labels,
  include.lowest = TRUE
)

fig <- ggplot(d) +
  geom_sf(aes(fill = class), color = "#ffffff", linewidth = 0.3) +
  geom_sf(data = comarca, fill = NA, color = "#36454f", linewidth = 0.45) +
  scale_fill_brewer(
    palette = "Blues",
    direction = 1,
    drop = FALSE,
    na.value = "#e8eaed",
    name = "% d'habitatge no principal (límits arrodonits)",
    guide = guide_legend(
      title.position = "top",
      title.hjust = 0.5,
      nrow = 2,
      byrow = TRUE,
      keywidth = grid::unit(9, "mm"),
      keyheight = grid::unit(4, "mm")
    )
  ) +
  coord_sf(datum = NA) +
  theme_void(base_size = 11) +
  theme(
    legend.title = element_text(face = "bold", size = 10, color = "#17202a"),
    legend.text = element_text(color = "#52616b", size = 8.5),
    legend.position = "bottom",
    legend.margin = margin(t = 2),
    plot.margin = margin(5, 5, 2, 5)
  )

output <- file.path(root, "assets/img/thematic-cartography/choropleth-normalized-reviewed-tarragones-2021.svg")
dir.create(dirname(output), showWarnings = FALSE, recursive = TRUE)
ggsave(output, fig, width = 4, height = 3.2, units = "in", bg = "white", device = svglite::svglite)
```

![Coropleta del percentatge d'habitatge no principal als 22 municipis del Tarragonès, amb cinc classes quantils]({{ site.baseurl }}/assets/img/thematic-cartography/choropleth-normalized-reviewed-tarragones-2021.svg "L'indicador normalitzat permet comparar municipis independentment de la mida del parc residencial. Les classes quantils reparteixen aproximadament el mateix nombre de municipis per classe; la llegenda mostra els límits arrodonits a una decimal.")
