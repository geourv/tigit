---
title: "Sèrie temporal: evolució de la població de Vila-seca (2000-2022)"
lang: ca
ref: population_vila_seca_series_line
format:
  gfm:
    output-file: population-vila-seca-series-line.generated.md
execute:
  echo: false
  warning: false
  message: false
execute-dir: project
unaltraweb_compute:
  engine: r
  mode: figure
  inputs:
    - _practices/tigit-projecte-territorial/data/raw/poblacio-vila-seca-2000-2022.csv
  outputs:
    - assets/img/data-visualization/project-population-vila-seca-2000-2022.svg
---

```{r}
find_project_root <- function(start) {
  path <- normalizePath(start, winslash = "/", mustWork = FALSE)
  repeat {
    if (file.exists(file.path(path, ".unaltraweb", "computations.yml"))) {
      return(path)
    }
    parent <- dirname(path)
    if (identical(parent, path)) {
      stop("Project root not found: missing .unaltraweb/computations.yml")
    }
    path <- parent
  }
}

root <- find_project_root(getwd())
library(ggplot2)

theme_tigit <- function(base_size = 12) {
  theme_minimal(base_size = base_size) +
    theme(
      panel.grid.minor = element_blank(),
      panel.grid.major = element_line(color = "#e6ebf0", linewidth = 0.35),
      axis.line = element_line(color = "#17202a", linewidth = 0.4),
      axis.ticks = element_line(color = "#17202a", linewidth = 0.3),
      axis.title = element_text(color = "#17202a", face = "bold"),
      axis.text = element_text(color = "#52616b"),
      plot.title = element_text(face = "bold", size = 13, hjust = 0, color = "#17202a"),
      plot.caption = element_text(size = 8, color = "#52616b", hjust = 0, margin = margin(t = 8)),
      legend.title = element_text(face = "bold", color = "#17202a"),
      legend.text = element_text(color = "#52616b"),
      legend.position = "top",
      plot.margin = margin(8, 12, 8, 8)
    )
}

series <- read.csv(
  file.path(root, "_practices/tigit-projecte-territorial/data/raw/poblacio-vila-seca-2000-2022.csv"),
  fileEncoding = "UTF-8"
)

format_es <- function(x) format(x, big.mark = ".", decimal.mark = ",", scientific = FALSE, trim = TRUE)

fig <- ggplot(series, aes(x = year, y = population)) +
  geom_line(color = "#2171b5", linewidth = 1) +
  geom_point(color = "#2171b5", size = 1.6) +
  scale_x_continuous(breaks = seq(2000, 2022, by = 2)) +
  scale_y_continuous(limits = c(0, NA), labels = format_es) +
  labs(
    title = "Evolució de la població de Vila-seca, 2000-2022",
    x = NULL,
    y = "Habitants",
    caption = "Font: Idescat, a partir del Padró continu de l'INE."
  ) +
  theme_tigit()

output <- file.path(root, "assets/img/data-visualization/project-population-vila-seca-2000-2022.svg")
dir.create(dirname(output), showWarnings = FALSE, recursive = TRUE)
ggsave(output, fig, width = 9, height = 5, device = svglite::svglite)
```